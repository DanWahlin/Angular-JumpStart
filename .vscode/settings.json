// Place your settings in this file to overwrite default and user settings.
{
    "files.exclude": {
      "**/app/**/*.js.map": true,
      "**/app/**/*.js": true,
      
      "**/config/*.js.map": true,
      "**/config/*.js": {
        "when": "$(basename).js.map"
      }
    }
}